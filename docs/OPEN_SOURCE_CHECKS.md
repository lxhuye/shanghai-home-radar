# 源码快照验证记录

日期：2026-09-10。检查对象为白名单导出的独立源码副本，不含原 `.git`、真实房源、
`data/evidence/sample/`、浏览器会话或运行缓存。原工作树和业务数据未用于测试写入。

## 已执行

| 检查 | 结果 |
| --- | --- |
| 非数据库测试：`tests/unit tests/api services/collector/tests` | 474 通过 |
| Ruff check | 通过 |
| Ruff format --check | 250 个文件合规 |
| 配置范围内 mypy | 134 个源码文件通过 |
| 全新 PostGIS 测试库迁移 | 0001–0009 成功 |
| `alembic check` | 无新迁移操作 |
| 数据库集成测试 | 37 通过；收集阶段另有 2 项可选测试跳过，不计为通过 |
| 前端 lint / typecheck / build | 全部通过；产出为静态演示页面 |
| Markdown 本地链接 | 44 处通过（检查时的文件集） |
| 两份 Compose 配置解析 | 通过；未启动业务栈 |
| Gitleaks v8.30.1 目录扫描 | 2 处告警，经人工核对为开发示例值与健康检查 URL；未发现真实凭据 |

Python 检查在已有 Python 3.12 测试镜像中离线运行。数据库测试使用新建内部网络、
独立临时数据库和内存数据目录，没有挂载真实数据库。前端检查先确认依赖锁文件与
已有镜像完全一致，再复制源码到一次性容器运行；没有重新从互联网安装依赖。

## 密钥扫描人工复核

- `docker-compose.yml` 的日更健康检查 URL 被 `generic-api-key` 识别；实际为内部 HTTP 路径，不含认证信息。
- `docs/DEVELOPMENT_GUIDE.md` 的 curl 示例被 `curl-auth-header` 识别；实际为明确标注的默认开发值 `dev-only-change-me`，不是个人凭据。

没有添加全局忽略规则或关闭扫描规则。两处示例不适用于生产安全配置。扫描结果不构成全面安全保证。

## 没有验证或不据此宣称

- 未测试真实来源的连续七天可用性、页面验证通过率或实际新增覆盖。
- 未补入真实成交、人工标签或未来因子，也未重跑冻结 P5.7。
- 未证明任何 Precision@K、估值误差或推荐质量；测试通过只证明相应软件行为。
- 本次未运行真实浏览器端到端用例；浏览器相关单元测试不能替代它。
- 未在公网部署，未进行完整依赖漏洞或渗透审计。

每次后续发布仍应在新快照上重跑检查，并以 GitHub 当前提交的 CI 结果为准。
