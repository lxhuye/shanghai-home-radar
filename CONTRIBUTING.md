# 贡献指南

当前优先级是可核查数据、统一日更交付和独立验证，见 [README](README.md)。

## 提交变更

1. 先描述问题、受影响链路以及可验收结果，再提交小范围分支或 PR。
2. 使用 Python 3.12 和 Node.js 22；运行 README 中的检查。数据库集成测试只操作独立测试库。
3. 修复附带回归测试。用明确标记的合成数据；不要提交真实挂牌页面、业主/经纪人信息、账户、Cookie 或抓取缓存。
4. 使用 Conventional Commits，例如 `fix(collector): preserve partial coverage`。
5. PR 说明测试实际运行结果、未运行项和数据边界；不要把测试通过写成生产或预测效果已验证。

## 不可破坏的约束

- `source_id / scope / data_mode / observed_at / completeness` 必须保真。
- 部分、失败和未知覆盖不能触发下架对账；首次观察不能冒充当日新增。
- 原始证据和冻结标签不可覆盖。重复来源记录应幂等，同键冲突应报错。
- 不自动破解验证码、不搬运日常浏览器会话、不通过隐藏重试规避来源访问限制。
- 不用合成证据提升真实房源置信度。模型参数变更不得影响已经冻结的盲测运行。
- 原始跟踪线索与达到模型门槛的候选分开；没有足够证据时允许不输出推荐。

来源适配的 PR 需说明字段映射、授权/使用边界、分页与覆盖证明、失败行为和测试方法，不要求附带供应商账号。贡献仅限自己有权许可的内容。
