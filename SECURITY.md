# 安全与私有数据

本项目当前按单人、自托管、本机或受控私网部署设计，不是面向公网的多用户 SaaS。

- 主 API 的读取接口依赖网络隔离，现有写入保护不等于完整认证授权体系。
- 主 Compose 使用开发口令，端口未全部限制到 loopback。公网部署前必须配置认证、TLS、网络隔离和独立密钥。
- 浏览器面板、noVNC 与会话控制端口仅供本机使用。不要暴露 WebDriver 或复用个人日常浏览器配置。
- 环境文件、令牌、Webhook 地址、会话、备份、实际房源、人工标签与个人偏好不应进入 Git、公开日志或 GitHub Issues。
- 开源代码许可不提供任何第三方网站、账号或数据的使用授权。

## 报告问题

若仓库启用了 GitHub 私密漏洞报告，请使用 Security 页的私密报告入口。否则先创建不含漏洞细节、凭据或个人数据的 Issue 请求私下联系；不要公开可利用细节或原始数据。

凭据若曾提交，删除当前文件不足以消除泄露，应由维护者撤销/轮换并检查历史及构建产物。首次公开发布采用经过审查的新快照，不直接公开本地研究历史。
